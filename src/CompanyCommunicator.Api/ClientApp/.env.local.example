# Frontend-Only Development
# Copy this file to .env.local and set your Azure API URL.
# .env.local is git-ignored, so your values stay private.

# Proxy API calls to your live Azure backend instead of localhost:5001
# Find your URL in the Azure Portal → App Service → Overview → Default domain
VITE_API_URL=https://cc-api-XXXXX.azurewebsites.net
